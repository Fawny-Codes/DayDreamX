# Environment Configuration for DayDream X

# Server Port (Railway, Heroku, etc. usually set this automatically)
PORT=8080

# Node Environment
NODE_ENV=production

# Optional: Add more configuration as needed
# DATABASE_URL=
# API_KEY=
